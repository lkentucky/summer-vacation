# 智能车调试对话记录（2026-07-25）

> 下次重新打开对话时，建议先读取本文件，以及旧记录：  
> `C:\Users\csz18\Desktop\智能车暑期集训\main\智能车调试对话记录_2026-07-22.md`

本次主要围绕：冲出赛道误判、IMU 方向抑制、转向环节拍、图像处理固定节拍、摄像头实际帧率、速度量纲和编码器速度标定。

---

## 1. 当前工程位置与主要文件

工程根目录：

```text
C:\Users\csz18\Desktop\智能车暑期集训\main
```

本次主要涉及这些文件：

```text
C:\Users\csz18\Desktop\智能车暑期集训\main\smartcarmain\user\src\main.c
C:\Users\csz18\Desktop\智能车暑期集训\main\smartcarmain\user\src\motor.c
C:\Users\csz18\Desktop\智能车暑期集训\main\smartcarmain\user\inc\motor.h
C:\Users\csz18\Desktop\智能车暑期集训\main\smartcarmain\user\src\isr.c
C:\Users\csz18\Desktop\智能车暑期集训\main\smartcarmain\user\inc\isr.h
C:\Users\csz18\Desktop\智能车暑期集训\main\smartcarmain\user\src\IMU.c
C:\Users\csz18\Desktop\智能车暑期集训\main\smartcarmain\user\inc\IMU.h
C:\Users\csz18\Desktop\智能车暑期集训\main\smartcarmain\user\src\mymenu.c
C:\Users\csz18\Desktop\智能车暑期集训\main\smartcarmain\user\src\image.c
```

---

## 2. 冲出赛道逻辑的修改

### 问题

原来的冲出赛道判断在十字、环岛等特殊赛道区域容易误判。现象是：

- 十字时左右边界可能同时丢失；
- 环岛或大面积白色区域时中线/边界异常；
- 旧逻辑会把这些当成“冲出赛道”，导致停车。

### 已做修改

在 `main.c` 中改了 `track_lost_detect()` 的思路：

- 不再只看左右边界是否丢失；
- 加入底部若干行的“可见赛道白色区域”判断；
- 如果画面底部仍有足够白色道路，则认为更可能是十字/环岛/特殊赛道，不直接判为冲出赛道；
- 仍然保留连续多帧确认，避免单帧误判。

相关参数大致为：

```c
#define TRACK_LOST_ROW_START       92
#define TRACK_LOST_ROW_END         115
#define TRACK_LOST_COUNT_TH        23
#define TRACK_LOST_CONFIRM_FRAMES  4
#define TRACK_START_GRACE_FRAMES   12
#define TRACK_WIDTH_MIN            20
#define TRACK_WIDTH_MAX            175
#define TRACK_EDGE_MARGIN          3
#define TRACK_ROAD_WHITE_RUN_MIN       28
#define TRACK_ROAD_WHITE_COUNT_MIN     45
#define TRACK_CENTER_WHITE_HALF_WIDTH  8
#define TRACK_CENTER_WHITE_COUNT_TH    6
```

启动后还有 `TRACK_START_GRACE_FRAMES` 宽限帧，避免刚启动图像不稳定时误停。

---

## 3. 加入 IMU 方向抑制

### 用户需求

车头摆动太大，希望加 IMU 抑制，并且姿态角解算相关代码写在 `IMU.c` 里。

### 已做修改

新增/整理了：

```text
smartcarmain\user\src\IMU.c
smartcarmain\user\inc\IMU.h
```

主要功能：

- 初始化 `imu963ra`；
- 上电/复位后静止采样陀螺仪零偏；
- `imu_update()` 周期更新陀螺仪和加速度计；
- pitch/roll 使用互补滤波；
- yaw 只积分，不作为长期可靠航向；
- 使用 Z 轴角速度做转向抑制。

当前关键变量：

```c
bool  imu_ready;
float imu_gyro_z_dps;
float imu_gyro_z_dps_filter;
float imu_pitch;
float imu_roll;
float imu_yaw;
float Kgyro_steer;
```

当前关键参数：

```c
#define IMU_GYRO_Z_FILTER_ALPHA (0.70f)
float Kgyro_steer = 0.45f;
```

方向抑制接口：

```c
float imu_get_steer_damping(void)
{
    if (!imu_ready) return 0.0f;
    return Kgyro_steer * imu_gyro_z_dps_filter;
}
```

### 注意事项

1. 上电/复位后，车必须静止一小段时间，让陀螺仪零偏校准正确。  
2. 当前 `motor.c` 中转向公式使用的是：

```c
float steering = Kp_steer * error_far
               + Kd_steer_position * preview_error
               + Kd_steer_time * d_error
               + imu_get_steer_damping();
```

如果发现加 IMU 后车头摆动更大，优先怀疑符号反了：

- 可以先把菜单里的 `Kgyro_steer` 调成负值；
- 或者把公式里的 `+ imu_get_steer_damping()` 改成 `- imu_get_steer_damping()`。

---

## 4. 转向环 PID 与图像处理节拍解耦

### 问题

用户指出：“转向环 PID 计算应该比图像处理快才行。”

这是正确的。图像处理一般受摄像头帧率限制，例如 50fps 时一帧 20ms；但电机/转向控制最好每 1~5ms 更新一次。

### 已做修改

把方向控制从图像处理主流程里拆出来：

- 图像处理只计算最新赛道误差；
- TIM6 中断以固定 2ms 节拍刷新转向控制和电机 PID。

`motor.c` 中新增/整理：

```c
void steering_set_image_error(int16 error_near, int16 error_far);
void steering_control_update(void);
```

`main.c` 中图像处理完成后只更新误差：

```c
steering_set_image_error(get_mid_error_average(STEER_NEAR_ROW_START, STEER_NEAR_ROW_END),
                         get_mid_error_average(STEER_FAR_ROW_START, STEER_FAR_ROW_END));
```

`isr.c` 的 TIM6 中断中执行：

```c
get_motor_speed();
steering_control_update();
motor_pid_speedcontrol();
```

`main.c` 中 TIM6 当前初始化为：

```c
pit_ms_init(TIM6_PIT, 2);   // TIM6: PID控速，2ms
```

`isr.h` 中定义：

```c
#define SYS_TICK_MS   (2)
#define SYS_TICK_SEC  (0.002f)
```

也就是说：

- 图像处理：按摄像头实际帧率来，大约 20ms 一帧时就是 50Hz；
- 转向环/速度环：TIM6 每 2ms 一次，也就是 500Hz；
- 转向环使用“上一帧图像误差 + 当前 IMU 滤波角速度”进行更快刷新。

---

## 5. 图像处理整理成固定节拍，并增加诊断量

### 用户需求

摄像头帧率明显偏低，需要把图像处理整理成固定节拍，方便判断多少毫秒一帧，并尽量提高频率。

### 已加诊断变量

`main.c` 中当前有：

```c
int image_period_ms;     // 图像处理最小间隔，单位 ms
int image_frame_ms;      // 实际两次处理之间的间隔，单位 ms
int image_proc_ms;       // 一次图像处理耗时，单位 ms
int image_fps;           // 实际处理帧率
int image_wait_count;    // 检查时摄像头还没给新帧的次数
```

菜单中也加入了图像诊断相关项，大致包括：

- `period_ms`
- `frame_ms`
- `proc_ms`
- `fps`
- `wait`

屏幕上也会显示类似：

```text
IMG xxms yyfps zzms
```

其中：

- `frame_ms`：两帧之间实际间隔；
- `fps`：根据 `frame_ms` 算出来的实际处理帧率；
- `proc_ms`：这一帧图像处理本身用了多久。

### 当前图像参数

`main.c` 中当前值：

```c
#define CAMERA_FPS_TARGET        100
#define CAMERA_EXPOSURE_TIME      200
#define CAMERA_GAIN_VALUE         45
#define IMAGE_PERIOD_MS_DEFAULT   20
#define IMAGE_PERIOD_MS_MIN       SYS_TICK_MS
#define IMAGE_PERIOD_MS_MAX       50
```

注意：`CAMERA_FPS_TARGET = 100` 只是给摄像头模块的目标值，不代表实际一定能到 100fps。

### 当前取帧逻辑

后来把 `STEP_IDLE` 改成了“低延迟取帧”：

```c
// 主循环持续检查 mt9v03x_finish_flag
// 有新帧且满足最小处理间隔就立即处理
```

也就是说，现在 `image_period_ms` 的含义是：

> 图像处理的最小间隔，而不是每隔这么久才去看一次摄像头。

这样可以降低取帧延迟。

---

## 6. 关于 fps 一直 50、wait 一直增加

### 现象

用户看到：

- `fps` 一直是 50；
- 因此把 `image_period_ms` 改成 20；
- 但车头还是抖，感觉图像太慢。

### 解释

如果实际 `fps = 50`，说明摄像头实际大约 20ms 才给一帧。

设置：

```c
CAMERA_FPS_TARGET = 100
```

不等于实际一定 100fps。实际帧率受这些限制：

- 摄像头模块/驱动本身；
- 分辨率；
- 曝光时间；
- 串口/并口传输；
- 图像读取方式；
- MT9V03X 模块内部固件限制。

### wait 正常应该是什么样

`wait` 增加的含义是：主循环去看摄像头时，摄像头还没有新帧。

在低延迟模式下，如果把 `image_period_ms` 设得很小，例如 2ms 或 4ms，而摄像头实际只有 50fps，那么主循环肯定会频繁“等不到新帧”，所以 `wait` 增加是正常的。

判断瓶颈时看这几个量：

| 现象 | 说明 |
|---|---|
| `frame_ms ≈ 20`, `fps ≈ 50`, `proc_ms` 很小 | 摄像头是瓶颈 |
| `proc_ms` 接近或超过 20ms | 图像处理代码太慢 |
| `image_period_ms` 设很大，`frame_ms` 也变大 | 是软件限频造成的 |

### 调参建议

如果摄像头实际只有 50fps，为了低延迟，不建议把 `image_period_ms` 固定设成 20。更建议：

```text
image_period_ms = 2 或 4
```

这样虽然 `wait` 会一直增加，但只要摄像头新帧一到，主循环就会尽快处理，延迟最低。

真正的图像刷新仍然受摄像头限制：

```text
实际 fps = min(摄像头实际fps, 处理限频, CPU处理能力)
```

---

## 7. 当前车头摆动的主要排查方向

现在结构上已经做到：

- 图像 50Hz 左右；
- 转向/电机 500Hz；
- 加了 IMU Z 轴角速度抑制；
- 图像取帧低延迟。

如果车头仍然抖，优先查这些：

### 1. 方向 P 太大

当前 `motor.c` 中：

```c
float Kp_steer = 5.5f;
float Kd_steer_position = 0.0f;
float Kd_steer_time = 0.01f;
float Kgyro_steer = 0.45f;
```

`Kp_steer = 5.5` 可能偏大，尤其速度较高时容易摆头。可以尝试：

```text
先降低 Kp_steer
再慢慢加 Kgyro_steer
```

### 2. IMU 抑制符号可能反

如果加 IMU 后更抖，试：

```text
Kgyro_steer 改成负数
```

或者改公式符号。

### 3. 图像远近行设置可能不合理

当前近、远行看起来都在 60~65 附近：

```c
#define STEER_NEAR_ROW_START  60
#define STEER_NEAR_ROW_END    65
#define STEER_FAR_ROW_START   60
#define STEER_FAR_ROW_END     65
```

如果近处和远处行一样，那么 `preview_error = error_far - error_near` 基本没有意义。后续可以重新设置：

```text
near：更靠近车底的行，例如 80~100
far ：更远的行，例如 45~65
```

具体要根据图像坐标方向和实际赛道显示来定。

### 4. 基础速度太高

当前：

```c
int run_base_speed = 200;
```

如果速度单位标定不准，实际速度可能和菜单值不一致。先把速度闭环标定准，再调方向。

---

## 8. 速度量纲：base_speed、target_speed、steering

### 当前理解

当前代码设计上希望：

```text
base_speed      单位：cm/s
real_speedl/r   单位：cm/s
target_speedl/r 单位：cm/s
steering        单位：cm/s 的左右轮速度差修正量
```

所以公式：

```c
target_speedl = base_speed + steering;
target_speedr = base_speed - steering;
```

这里的 `steering` 不应该理解成 PWM 占空比，而是“左右轮目标速度差的一半/修正量”，单位应理解为 cm/s。

### 为什么 steering 看起来像 PWM

因为 `steering` 是由像素偏差算出来的：

```c
float steering = Kp_steer * error_far
               + Kd_steer_position * preview_error
               + Kd_steer_time * d_error
               + imu_get_steer_damping();
```

这里：

- `error_far` 单位是像素；
- `d_error` 单位也是像素差；
- `imu_gyro_z_dps_filter` 单位是 °/s；
- 通过 `Kp_steer`、`Kd_steer_time`、`Kgyro_steer` 把它们经验映射成 cm/s 的速度修正。

因此这些增益的量纲并不是纯数学无量纲，而是需要实车调出来的换算系数。

### 建议后续重命名

为了减少误解，后续可以把变量改名为：

```text
base_speed       -> base_speed_cm_s
run_base_speed   -> run_base_speed_cm_s
steering         -> speed_delta_cm_s
target_speedl    -> target_speedl_cm_s
target_speedr    -> target_speedr_cm_s
real_speedl      -> real_speedl_cm_s
real_speedr      -> real_speedr_cm_s
```

本次还没有做这个重命名。

---

## 9. 为什么算出来的速度比真实速度快

用户反馈算出来的速度比真实速度快。

当前 `motor.c` 中速度换算代码为：

```c
#define D 6.5
#define PPR 1024

real_speedl = (float)motor_speedl / PPR / 68 * 30 * (D * PI);
real_speedr = -(float)motor_speedr / PPR / 68 * 30 * (D * PI);
```

注意 C 语言中 `*` 和 `/` 同优先级，按从左到右计算，所以上式等价于：

```c
real_speed = motor_speed * (D * PI) * 30 / (PPR * 68);
```

如果 `68:30` 是齿轮/传动换算，这里相当于乘以 `30/68`。

### 常见导致速度算快的原因

1. **PPR 不等于真实有效每轮脉冲数**  
   当前用的是：

   ```c
   #define PPR 1024
   encoder_quad_init(...)
   ```

   如果是正交编码器四倍频，实际每圈计数可能是：

   ```text
   1024 * 4 = 4096
   ```

   如果代码里还用 1024，算出来速度会约快 4 倍。

2. **编码器装在电机轴，不在轮轴**  
   如果编码器在电机轴，还要除以减速比/传动比。

3. **轮径 D 不准**  
   当前：

   ```c
   #define D 6.5
   ```

   轮径偏大，速度也会算大。

4. **实际计数方向/符号左右不一致**  
   右轮现在有负号：

   ```c
   real_speedr = -(float)motor_speedr ...
   ```

   这个主要影响方向，不影响绝对值标定，但也要确认。

### 最可靠的标定方法

把车架起来，手动让轮子准确转一圈：

1. 清空编码器计数；
2. 让轮子正好转 1 圈；
3. 读取累计脉冲数；
4. 这个累计数就是“每轮一圈的有效 PPR”。

然后速度公式尽量写成：

```c
#define WHEEL_DIAMETER_CM       6.5f
#define WHEEL_CIRCUMFERENCE_CM  (WHEEL_DIAMETER_CM * PI)
#define PULSE_PER_WHEEL_REV     实测值

real_speed = pulse_per_second / PULSE_PER_WHEEL_REV * WHEEL_CIRCUMFERENCE_CM;
```

这样最不容易混乱。

---

## 10. Keil 编译问题记录

曾出现错误：

```text
Error: L6449E: While processing .\objects\zf_device_imu963ra.o:
I/O error writing file '.\objects\zf_device_imu963ra.o': Invalid argument
```

当时判断这大概率不是 C 语法错误，而是 Keil/Windows 文件系统相关问题，例如：

- 中文路径；
- `.o` 文件被 Keil/杀毒软件/同步软件锁住；
- objects 输出目录异常；
- 工程路径太复杂；
- Keil clean 没清干净。

后来用户说“不用改编译问题了”，所以本次没有继续处理这个问题。

---

## 11. KEY4 不能启动的问题记录

之前讨论过 KEY4 启动。当前需要注意：

- `base_speed` 当前是实际运行速度，0 表示停车；
- `run_base_speed` 是菜单可调启动速度；
- KEY4 启动时应该把 `run_base_speed` 赋给 `base_speed`；
- 停车保护/冲出赛道保护会把 `base_speed` 置 0；
- 如果刚启动马上被冲出赛道保护触发，会表现得像 KEY4 没启动。

目前已经加了启动宽限帧：

```c
#define TRACK_START_GRACE_FRAMES 12
```

如果 KEY4 仍然不能启动，下次优先检查：

1. KEY4 的按键分支是否进入；
2. `run_base_speed` 是否大于 0；
3. `base_speed` 是否刚赋值又被保护逻辑清零；
4. 屏幕状态字符串是否显示 `RUNNING` 或 `TRACK LOST STOP`；
5. 电机 PID 是否因为目标速度/实际速度标定异常导致无输出。

---

## 12. 当前关键参数快照

### main.c

```c
#define CAMERA_FPS_TARGET        100
#define CAMERA_EXPOSURE_TIME      200
#define CAMERA_GAIN_VALUE         45
#define IMAGE_PERIOD_MS_DEFAULT   20
#define IMAGE_PERIOD_MS_MIN       SYS_TICK_MS
#define IMAGE_PERIOD_MS_MAX       50

pit_ms_init(TIM6_PIT, 2);
```

### isr.h

```c
#define SYS_TICK_MS   (2)
#define SYS_TICK_SEC  (0.002f)
```

### motor.c

```c
#define D 6.5
#define PPR 1024
#define STEER_MAX_RATIO (0.85f)
#define STEER_MAX_STEP  (15.0f)

float Kp = 9.36f;
float Ki = 0.5f;
float Kd = 0.01f;

int base_speed = 0;
int run_base_speed = 200;
float Kp_steer = 5.5f;
float Kd_steer_position = 0.0f;
float Kd_steer_time = 0.01f;
```

### IMU.c

```c
#define IMU_GYRO_Z_FILTER_ALPHA (0.70f)
float Kgyro_steer = 0.45f;
```

---

## 13. 下次继续调试建议顺序

建议按这个顺序，不要同时改太多东西：

1. **先确认速度闭环量纲**  
   标定 `PPR` / 有效每轮脉冲数，让 `real_speed` 接近真实 cm/s。

2. **确认摄像头实际帧率**  
   看 `frame_ms`、`fps`、`proc_ms`：
   - 如果 `fps=50` 且 `frame_ms=20`，说明摄像头就是 50fps；
   - `image_period_ms` 建议设 2 或 4，降低等待延迟。

3. **低速调方向 P**  
   先把 `run_base_speed` 降下来，只调 `Kp_steer` 到不发散。

4. **再加 IMU 抑制**  
   慢慢调 `Kgyro_steer`。如果更抖，先反号。

5. **再考虑预瞄/微分**  
   调整 `STEER_NEAR_ROW` 和 `STEER_FAR_ROW`，让近远误差真正不同。

6. **最后提速**  
   速度越高，对图像延迟、方向增益、IMU 符号越敏感。

---

## 14. 一句话总结

本次已经把“图像慢”和“转向控制慢”分开了：图像按摄像头新帧更新，转向环由 TIM6 每 2ms 用上一帧图像误差和 IMU 角速度快速刷新。下一步最重要的是确认速度标定和摄像头实际帧率，然后调小方向 P、检查 IMU 抑制符号。
