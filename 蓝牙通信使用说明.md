# HC-04 双模蓝牙通信使用说明

## 接线

| 智能车接口 | HC-04 |
|---|---|
| C6（UART6 TX） | RXD |
| C7（UART6 RX） | TXD |
| C13 | 不接，保持空闲（未连接高电平，连接后低电平） |
| GND | GND |
| 稳定的 3.3V | VCC |

汇承 HC-04 裸模块要求 DC 3.0～3.6V，推荐 3.3V，供电能力不小于 100mA。U7 上标注的 5V 不能直接接裸模块；应增加 3.3V 稳压器。只有明确标注支持 5V 输入的转接底板才可以接 5V。

## 启动与配对

固件启动时会先尝试 115200 波特率；如果检测到模块仍是出厂 9600N81，会自动发送不带回车换行的 `AT+BAUD=115200`，模块重启后改用 115200。该设置会保存在 HC-04 内部。

- 经典蓝牙 SPP 名称：通常为 `HC-04`，默认配对码 `1234`。
- BLE 名称：通常为 `HC-04LE`，一般没有配对密码。
- Android 可使用支持 SPP 或 BLE 透传的蓝牙串口助手。
- Windows 使用经典蓝牙配对后生成的 SPP 虚拟 COM 口。

Windows 调试终端：

```powershell
python -m pip install pyserial
python main/tools/bluetooth_client.py --list
python main/tools/bluetooth_client.py COM8
```

手机蓝牙串口助手需要设置“发送时追加换行”。

## 命令

| 命令 | 示例 | 作用 |
|---|---|---|
| `PING` | `PING` | 返回 `OK,PONG` |
| `STATUS` | `STATUS` | 立即读取运行状态 |
| `GET 参数名` | `GET VISION_KP` | 读取参数 |
| `SET 参数名 数值` | `SET VISION_KP 5.5` | 修改运行时参数 |
| `STREAM ON/OFF` | `STREAM OFF` | 开关周期遥测 |
| `RATE 毫秒` | `RATE 200` | 设置遥测周期 |
| `START` | `START` | 启动车辆 |
| `STOP` | `STOP` | 立即停车并清空控制器状态 |
| `HELP` | `HELP` | 显示命令概要 |

可调参数包括：`SPEED_KP`、`SPEED_KI`、`SPEED_KD`、`RUN_SPEED`、`VISION_KP`、`VISION_KD`、`VISION_FF`、`YAW_KP`、`YAW_SIGN`、`YAW_MAX`、`STRAIGHT_SPEED`、`CORNER_SPEED`、`STRAIGHT_VKP`、`CORNER_VKP`、`STRAIGHT_YKP`、`CORNER_YKP`、`ENTER_PX`、`EXIT_PX`、`ACCEL_STEP`、`DECEL_STEP`。

参数修改仅在本次上电期间生效。

## 遥测

连接蓝牙并发送 `STREAM ON` 后，固件默认每 200ms 发送一条短数据：

```text
B,t=12540,run=1,b=280,tl10=2850,tr10=2750,vl10=2812,vr10=2726,e=3,g10=126,yr10=140,fps=50,fms=20,st=0,cr=0,zb=1
```

带 `10` 后缀的字段是原值乘以10后的整数。若自动波特率配置失败并退回9600，默认发送周期会自动降低到1000ms，避免占用主循环过多时间。

## 无STATE引脚模式

当前硬件没有连接STATE，因此固件上电默认关闭连续回传。蓝牙连接后发送 `STREAM ON` 开始回传；断开前可发送 `STREAM OFF`。模块断开后若返回ERR，固件也会自动停止回传。

## 摇杆遥控数据包

格式：`[joystick,转向,前后,备用1,备用2]`，四个数范围均为-100到100。

- 第1个数：负数左转，正数右转，绝对值越大转向越快；转向差速为原始映射的25%。
- 第2个数：正数前进，负数后退，100对应2m/s，-100对应-2m/s。
- 第3、4个数：当前接收并校验，但暂不参与控制。
- 中心死区：-5到5按0处理。
- 安全保护：连续300ms收不到新摇杆包，自动停车。

示例：`[joystick,-50,80,0,0]` 表示前进速度指令160cm/s、转向差速分量25cm/s，左右轮目标约135/185cm/s。
## ST/CR实时曲线

发送 `DRAW` 或 `DRAW ON` 后，固件自动开启数据流，并按RATE周期发送：`[plot,st,cr]`。

- 第1条曲线st：0直道、1弯道、2摆动抑制。
- 第2条曲线cr：0普通巡线、1十字补线。
- `DRAW OFF`：退出绘图格式，恢复原来的B格式遥测。
- `STREAM OFF`：停止所有周期回传。
- `RATE 100`：设置绘图周期为100ms。

绘图包严格使用方括号格式且不附加换行，例如：`[plot,1,0]`。