# new_version 移植说明

## 代码来源

- `main.c`、`mymenu.c`、`motor.c`、`image.c`、`cross.c` 及对应的 `motor.h`、`image.h`、`cross.h`：来自 `main/rewrite_backup_20260809`。
- `isr`、`IMU`、`bluetooth_app`、`key`、`menu`、`calculation`：来自当前 `main/smartcarmain`，用于补齐原备份代码依赖的硬件中断和辅助模块。
- Keil 工程、J-Link 配置、CMake 配置和引脚分配文档：与当前 `main/smartcarmain` 对齐。

原 `new_version` 的屏幕演示主程序、ISR 和工程文件保存在 `port_backup_20260810`。

## 引脚对齐结果

- 左电机方向：A0
- 右电机方向：A1
- 左电机 PWM：TIM5 CH3 / A2
- 右电机 PWM：TIM5 CH4 / A3
- 左编码器：TIM3 / B4、B5
- 右编码器：TIM4 / B6、B7
- MT9V03X：UART8、TIM1、E6、E7、E8-E15
- IPS200 SPI：A4、A5、A6、A7、D0、D1
- IMU963RA SPI2：B12、B13、B14、B15
- HC-04 蓝牙 UART6：C6 TX、C7 RX；C13 未使用

以上配置与 `main/smartcarmain` 相同。完整表格见 `MM32F327X_G8P智能车推荐引脚分配.txt`。

## 构建验证

Keil ARMCC 5.06 update 7 构建结果：

- 0 errors
- 1 warning：逐飞库 `hal_iwdg.c` 文件末尾没有换行，与本次移植代码无关
- Code：54604 bytes
- RO-data：42140 bytes
- RW-data：1372 bytes
- ZI-data：81800 bytes

烧录产物位于 `mdk/Objects`：

- `MM32F327X_G8P.axf`
- `MM32F327X_G8P.hex`
- `MM32F327X_G8P.bin`

## 备注

备份中的环岛函数代码被原样保留，但 `main.c` 中的环岛处理入口仍处于注释状态，不会在当前主流程中执行。
