# F车走马观碑项目说明

## 工程范围

本工程只使用原 `main` 项目已有硬件：

- MM32F3273G8P
- MT9V03X 灰度摄像头
- 左右双电机与 TIM3/TIM4 正交编码器
- IMU963RA
- IPS200、四按键
- UART6 HC-04 蓝牙

不包含环岛、负压、激光、舵机、TOF 或参考 C 车的其他执行机构。

## 控制流程

1. 对摄像头有效区域计算 OTSU 阈值，并限制单帧阈值跳变。
2. 根据各行平均亮度修正该行阈值，减小阴影和上下亮度不均的影响。
3. 从图像底部白色连通段寻找起点，逐行连续搜索左右边界；所有搜索均有显式边界检查。
4. 双边有效时学习每一行的透视路宽；单边丢失时按该行历史路宽恢复中线。
5. 十字入口连续确认后，用开阔区上方的左右远点和车前近点补线；恢复普通双边后退出。
6. 根据车速选择前瞻行，将前瞻横向误差换算为物理横向距离，按纯跟踪公式得到路径曲率。
7. 路径曲率生成目标角速度，IMU Z 轴角速度内环将其换算为左右轮目标速度差。
8. 左右轮独立增量式 PID 跟踪目标速度；速度按曲率和图像置信度分四档，并执行快减慢加。
9. 斑马线采用底部多行模式、上方双边保护、连续帧确认和离开锁存；通过第二条后停车。
10. 图像置信度连续过低且画面下方无连续白色道路时停车；十字开阔区不会直接触发丢线停车。

## 首次上车顺序

1. 先架空车轮，双击 K4 启动后确认左右轮正转、编码器换算速度均为正。
2. 手动转动车轮一整圈，核对累计计数。若不是约 `1024*68/30`，修改 `motor.c` 的 `ENCODER_COUNTS_PER_WHEEL_REV`。
3. 将四档速度临时降到安全值，先验证直道、弯道、十字和第二条斑马线停车。
4. 若转向方向相反，只改 `speed_straight_yaw_feedback_sign` 与 `speed_corner_yaw_feedback_sign` 的符号。
5. 低速稳定后依次调整纯跟踪倍率、航向前馈、角速度内环，再逐档提高速度。

## 主要可调参数

- `speed_0..speed_3`：由直道到急弯的四档速度，必须保持从高到低。
- `pure_k_str / pure_k_cur`：纯跟踪目标角速度倍率。
- `heading_ff`：目标点相对近处中心的航向前馈。
- `kp_str / kp_cur`：IMU 角速度内环比例。
- `sign_str / sign_cur`：陀螺仪安装方向修正，通常为 `1` 或 `-1`。
- `width_k`：单边中线恢复宽度倍率，建议保持在 `0.70..1.30`。
- `accel / decel`：每个图像帧的最大加速/减速量。

## 构建结果

已使用 Keil ARMCC 5.06 update 7 编译 `smartcarmain/mdk/mm32f327x_g8p.uvprojx`：

- 0 errors，0 warnings
- Code 53948 bytes
- RO-data 42140 bytes
- RW-data 1212 bytes
- ZI-data 82048 bytes

可烧录文件由 Keil 工程输出到 `smartcarmain/mdk/Objects`。
